# Supply chain defense: exclude-newer is set dynamically via UV_EXCLUDE_NEWER
# in .zshrc (rolling 1-day window). uv.toml doesn't support relative durations.
# https://daniakash.com/posts/simplest-supply-chain-defense/
