# Cross-machine skill sources, tracked in this repo.
# Installed by scripts/install-skills.sh alongside the script's built-in defaults.
#
# Format: one entry per line, `#` comments and blank lines ignored.
#   <source> [skill-name ...]
# `<source>` is anything the `skills` CLI accepts (github org/repo, git URL, path).
# Skill names default to `*` (install everything from the source).
#
# For work-only or machine-specific sources, use ~/.skills.local instead
# (untracked, same format, runs with DISABLE_TELEMETRY=1).

# Personal skills repo (github.com/superhighfives/skills)
# Use the SSH URL rather than the `owner/repo` shortname: the shortname makes the
# skills CLI resolve the repo via the GitHub API, which trips the anonymous
# 60/hr rate limit and drops into an interactive `gh` login prompt that hangs a
# non-interactive run. The SSH URL just `git clone`s (SSH key auth), no API call.
git@github.com:superhighfives/skills.git dependabot-bundle review-github review-gitlab writing-change-descriptions
